{% extends 'base.html.twig' %}

{% block body %}
<h1>{{ form.vars.value.id ? 'Edit Author' : 'New Author' }}</h1>
{{ form_start(form) }}
{{ form_widget(form) }}
<button type="submit">{{ form.vars.value.id ? 'Update' : 'Create' }}</button>
{{ form_end(form) }}
{% endblock %}
